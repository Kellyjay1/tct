version: "1"
services:
  - type: web
    name: tct-0.0.0
    runtime: docker
    plan: free
    region: virginia
    dockerContext: .
    dockerfilePath: ./Dockerfile
    autoDeployTrigger: commit

    envVars:
      - key: SESSION_ID
        sync: tct_cdd39401
      - key: TIMEZONE
        sync: Africa/Lagos
      - key: PREFIX
        sync: mine
      - key: POSTGRES_URL
        sync: false
